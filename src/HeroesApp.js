import React from 'react';
import PropTypes from 'prop-types';

const HeroesApp = () => {
    return (
        <div>
            <h1>HeroesApp</h1>
        </div>
    );
};

HeroesApp.propTypes = {

};

export default HeroesApp;
